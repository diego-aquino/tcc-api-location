{
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "devCommand": "pnpm dev",
  "buildCommand": "pnpm build && cp -r dist api",
  "trailingSlash": false,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
