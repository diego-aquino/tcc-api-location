{
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "devCommand": "pnpm dev",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "trailingSlash": false,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist"
    }
  ]
}
